# secrets — NEVER commit
.env
.env.*
!.env.example
*.key
hot.json
cold.json
treasury*.json
secret*.json

# deps + noise
node_modules/
npm-debug.log*
.DS_Store
dbcheck.sh

# operator tooling — takes DB URLs on the command line, writes audits full of player data.
# Lives in the dev copy (~/Downloads/chiki-backend) and must never be deployed.
roster_merge.mjs
roster_merge_audit_*.json
fix_collection.mjs
authority.json
create_collection.mjs
make_keyfile.mjs
update_collection.mjs
upload_image.mjs
